services:
  - type: web
    name: static-upload
    runtime: node
    plan: free
    region: oregon
    buildCommand: ""
    startCommand: node server.js
    healthCheckPath: /api/status
    envVars:
      - key: NODE_ENV
        value: production
